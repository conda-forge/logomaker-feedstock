{% set name = "logomaker" %}
{% set version = "0.8.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/logomaker-{{ version }}.tar.gz
  sha256: 42c89a2e94ac6dd49a4ae4f4e3f6ce538a5def633b9f13a0e2d5d8750662eaef

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - matplotlib-base
    - pandas

test:
  imports:
    - logomaker
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: http://logomaker.readthedocs.io
  summary: Package for making Sequence Logos
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - hmacdope
    - mikemhenry
